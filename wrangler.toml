name = "netd-stock-app"
compatibility_date = "2024-09-01"
pages_build_output_dir = "."

# Cloudflare D1 Database Binding
# Bound directly in Cloudflare Pages Dashboard:
# Go to Settings > Functions > D1 database bindings > Variable name: DB -> select your D1 DB.
# (Do NOT put placeholder UUID here, as Cloudflare will fail deployment with Error 8000022)
